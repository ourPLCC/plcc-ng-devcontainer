# plcc-ng-devcontainer Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Repurpose this clone of plcc-devcontainer into `plcc-ng-devcontainer`: a published devcontainer feature that installs plcc-ng from PyPI, plus two prebuilt multi-arch images (standard and `-full`), per the spec at `docs/superpowers/specs/2026-07-14-plcc-ng-devcontainer-design.md`.

**Architecture:** One canonical feature at `src/plcc-ng/` (published to `ghcr.io/ourplcc/features/plcc-ng`; a sync script copies it into each image workspace so image builds always test the local feature source). Two image workspaces under `images/` composed from official language features plus the local plcc-ng feature, built with the devcontainer CLI, tested with a shared smoke script, and published multi-arch (native amd64 + arm64 runners, stitched with `docker buildx imagetools create`). semantic-release drives versioning exactly as in the old repo.

**Tech Stack:** devcontainer CLI (`@devcontainers/cli`), Docker/buildx, GitHub Actions (incl. `ubuntu-24.04-arm` native runners), semantic-release (`cycjimmy/semantic-release-action@v4`), GHCR, `jq`, `gh` CLI, PyPI JSON API, REUSE.

## Global Constraints

- **No remote:** this repo has no git remote yet. Commit locally; NEVER push. Bootstrap of the GitHub repo is a documented maintainer step (Task 7 docs), not part of this plan.
- **Prerequisites for executing this plan:** Docker running locally (`docker info` succeeds) and the devcontainer CLI (`npm install -g @devcontainers/cli`). The `plcc-ng-full` image build downloads a multi-GB Haskell toolchain: expect 20–40 minutes and ~15 GB free disk.
- **Names (exact, everywhere):** feature `ghcr.io/ourplcc/features/plcc-ng`; images `ghcr.io/ourplcc/devcontainers/plcc-ng` and `ghcr.io/ourplcc/devcontainers/plcc-ng-full`; GitHub repo URL `https://github.com/ourPLCC/plcc-ng-devcontainer`.
- **Pinned versions (exact):** plcc-ng `1.0.0` (current PyPI release; requires Python >=3.12); Java feature option `"21"`; Python feature option `"3.12"`; Node feature option `"lts"`; Haskell feature `ghcr.io/devcontainers-extra/features/haskell:2` (defaults).
- **plcc-ng commands** (console scripts from PyPI package `plcc-ng`): `plcc-version`, `plcc-make`, `plcc-scan`, `plcc-parse`, `plcc-rep` (among ~39 total).
- **Conventional Commits** for every commit message (semantic-release reads them).
- **Licensing:** `.sh`/`.yml` files carry the two-line SPDX header used throughout this repo (`GPL-3.0-or-later`); Markdown under `docs/` and JSON files are covered by `REUSE.toml` annotations — no header needed.
- **Workflow validation:** no way to run Actions locally; validate every workflow file with actionlint: `docker run --rm -v "$PWD":/repo --workdir /repo rhysd/actionlint:latest -color`.

---

### Task 1: Repository identity reset

**Files:**
- Modify: `CHANGELOG.md` (reset)
- Modify: `REUSE.toml:2`
- Local git state: delete inherited tags

**Interfaces:**
- Produces: a repo where semantic-release will compute the first release as `1.0.0` (no tags), under the package name `plcc-ng-devcontainer`.

- [ ] **Step 1: Delete inherited git tags**

Run: `git tag -l | xargs git tag -d`
Expected: lines like `Deleted tag 'v1.0.0' ...` through `'v1.1.0'`.
Run: `git tag -l`
Expected: no output. (These are local-only deletions; there is no remote. semantic-release finds no tag and starts at 1.0.0.)

- [ ] **Step 2: Reset CHANGELOG.md**

Replace the entire content of `CHANGELOG.md` with:

```markdown
# Changelog

Generated by [semantic-release](https://github.com/semantic-release/semantic-release)
from [Conventional Commits](https://www.conventionalcommits.org/) on `main`.
```

- [ ] **Step 3: Update REUSE package name**

In `REUSE.toml`, change line 2:

```toml
SPDX-PackageName = "plcc-ng-devcontainer"
```

- [ ] **Step 4: Commit**

```bash
git add CHANGELOG.md REUSE.toml
git commit -m "chore: reset repository identity for plcc-ng-devcontainer"
```

---

### Task 2: The plcc-ng feature (TDD) and repo dev environment

**Files:**
- Create: `src/plcc-ng/devcontainer-feature.json`
- Create: `src/plcc-ng/install.sh`
- Test: `test/plcc-ng/test.sh`, `test/plcc-ng/scenarios.json`, `test/plcc-ng/pinned_version.sh`
- Delete: `.devcontainer/features/plcc/` (old git-clone feature)
- Modify: `.devcontainer/devcontainer.json` (repo's own dev environment)

**Interfaces:**
- Produces: feature id `plcc-ng` with one option `version` (string, default `"latest"`, PyPI version like `"1.0.0"`); installs console scripts into `/usr/local/bin` (on default PATH — no PATH manipulation needed); pipx venv at `/usr/local/pipx/venvs/plcc-ng`; `dependsOn` pulls `ghcr.io/devcontainers/features/python:1 {"version":"3.12"}` so consumers do NOT list the python feature themselves. Task 3's images reference it as local feature key `"./features/plcc-ng"`.

- [ ] **Step 1: Verify prerequisites**

Run: `docker info > /dev/null && devcontainer --version`
Expected: a CLI version like `0.8x.x`. If `devcontainer` is missing: `npm install -g @devcontainers/cli`.

- [ ] **Step 2: Write the failing feature tests**

Create `test/plcc-ng/test.sh` (then `chmod +x`):

```bash
#!/bin/bash
# SPDX-FileCopyrightText: 2026 ourPLCC contributors
# SPDX-License-Identifier: GPL-3.0-or-later
# Feature test run by `devcontainer features test` against the default options.
set -e

source dev-container-features-test-lib

check "plcc-version on PATH" command -v plcc-version
check "plcc-version runs" plcc-version
check "plcc-make on PATH" command -v plcc-make
check "Python 3.12+ present (via dependsOn)" bash -c "python3 --version | grep -E '3\.(1[2-9]|[2-9][0-9])'"

reportResults
```

Create `test/plcc-ng/scenarios.json`:

```json
{
    "pinned_version": {
        "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
        "features": {
            "plcc-ng": { "version": "1.0.0" }
        }
    }
}
```

Create `test/plcc-ng/pinned_version.sh` (then `chmod +x`):

```bash
#!/bin/bash
# SPDX-FileCopyrightText: 2026 ourPLCC contributors
# SPDX-License-Identifier: GPL-3.0-or-later
# Scenario: the version option pins the exact PyPI release.
set -e

source dev-container-features-test-lib

check "plcc-version runs" plcc-version
check "exact version installed" bash -c \
    "/usr/local/pipx/venvs/plcc-ng/bin/python -m pip show plcc-ng | grep -q '^Version: 1.0.0$'"

reportResults
```

- [ ] **Step 3: Run the tests to verify they fail**

Run: `devcontainer features test --project-folder . --features plcc-ng --base-image mcr.microsoft.com/devcontainers/base:ubuntu`
Expected: FAIL — the CLI cannot find feature `plcc-ng` under `./src` (error mentions the missing feature folder).

- [ ] **Step 4: Write the feature**

Create `src/plcc-ng/devcontainer-feature.json`:

```json
{
    "id": "plcc-ng",
    "version": "1.0.0",
    "name": "PLCC-NG",
    "description": "Installs plcc-ng (Programming Language Compiler Compiler, next generation) from PyPI.",
    "documentationURL": "https://github.com/ourPLCC/plcc-ng-devcontainer",
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "proposals": ["latest", "1.0.0"],
            "description": "PyPI version of plcc-ng to install (e.g. '1.0.0'), or 'latest' for the newest release."
        }
    },
    "dependsOn": {
        "ghcr.io/devcontainers/features/python:1": { "version": "3.12" }
    }
}
```

Create `src/plcc-ng/install.sh` (then `chmod +x`):

```bash
#!/bin/bash
# SPDX-FileCopyrightText: 2026 ourPLCC contributors
# SPDX-License-Identifier: GPL-3.0-or-later
# Installs plcc-ng from PyPI. Runs as root during devcontainer image build.
set -euo pipefail

VERSION="${VERSION:-latest}"

SPEC="plcc-ng"
if [ "$VERSION" != "latest" ]; then
    SPEC="plcc-ng==${VERSION}"
fi

# Install globally: apps in /usr/local/bin (default PATH), venvs under /usr/local/pipx.
export PIPX_HOME=/usr/local/pipx
export PIPX_BIN_DIR=/usr/local/bin

# The python feature (declared in dependsOn) installs pipx at
# /usr/local/py-utils/bin, which is not on PATH during image build.
PIPX="$(command -v pipx || true)"
if [ -z "$PIPX" ] && [ -x /usr/local/py-utils/bin/pipx ]; then
    PIPX=/usr/local/py-utils/bin/pipx
fi

if [ -n "$PIPX" ]; then
    "$PIPX" install "$SPEC"
else
    # Fallback when a base image provides python but no pipx.
    python3 -m pip install "$SPEC" \
        || python3 -m pip install --break-system-packages "$SPEC"
fi
```

- [ ] **Step 5: Run the tests to verify they pass**

Run: `devcontainer features test --project-folder . --features plcc-ng --base-image mcr.microsoft.com/devcontainers/base:ubuntu`
Expected: PASS for both the default test and the `pinned_version` scenario (several minutes: it builds test images, installing the python feature and plcc-ng). If the `check "exact version installed"` fails, inspect with `docker run --rm <test-image> ls /usr/local/pipx/venvs` — the venv path must match `install.sh`'s `PIPX_HOME`.

- [ ] **Step 6: Remove the old feature and repoint the repo's own devcontainer**

```bash
git rm -r .devcontainer/features
```

Replace the entire content of `.devcontainer/devcontainer.json` (this is the environment for people hacking on THIS repo — it needs node for the devcontainer CLI and docker to build/test images and features):

```json
{
    "name": "plcc-ng-devcontainer (repo development)",
    "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
    "features": {
        "ghcr.io/devcontainers/features/node:1": {},
        "ghcr.io/devcontainers/features/docker-in-docker:2": {}
    },
    "postCreateCommand": "npm install -g @devcontainers/cli"
}
```

- [ ] **Step 7: Commit**

```bash
git add src test/plcc-ng .devcontainer
git commit -m "feat: add plcc-ng feature installed from PyPI via pipx"
```

---

### Task 3: Image definitions, smoke test, build scripts, size measurement

**Files:**
- Create: `images/plcc-ng/.devcontainer/devcontainer.json`, `images/plcc-ng/.devcontainer/Dockerfile`
- Create: `images/plcc-ng-full/.devcontainer/devcontainer.json`, `images/plcc-ng-full/.devcontainer/Dockerfile`
- Create: `scripts/sync-features.sh`, `scripts/build-and-test.sh`
- Test: `test/smoke-test.sh` (replaces `test/test-env.sh`)
- Delete: `test/test-env.sh`
- Modify: `.gitignore`, `devcontainer.json` (root template)

**Interfaces:**
- Consumes: feature `src/plcc-ng` (Task 2), synced to `images/<variant>/.devcontainer/features/plcc-ng` (gitignored copy).
- Produces: `bash scripts/build-and-test.sh <plcc-ng|plcc-ng-full> [image-tag]` — builds the variant, runs the smoke test inside it, tags it (default tag `ghcr.io/ourplcc/devcontainers/<variant>:dev`). Workflows in Tasks 4–5 call exactly this script. `test/smoke-test.sh <variant>` is the in-container test contract. The pinned plcc-ng version lives at jq path `.features["./features/plcc-ng"].version` in BOTH image configs (Task 6's weekly bump edits both).

- [ ] **Step 1: Write the smoke test (replaces test-env.sh)**

```bash
git rm test/test-env.sh
```

Create `test/smoke-test.sh` (then `chmod +x`):

```bash
#!/bin/bash
# SPDX-FileCopyrightText: 2026 ourPLCC contributors
# SPDX-License-Identifier: GPL-3.0-or-later
# Smoke test: verifies a prebuilt image has all required tools.
# Run inside the container: smoke-test.sh <plcc-ng|plcc-ng-full>
set -euo pipefail

VARIANT="${1:?usage: smoke-test.sh <plcc-ng|plcc-ng-full>}"

PASS=0
FAIL=0

check() {
    local label="$1"
    local cmd="$2"
    if eval "$cmd" > /dev/null 2>&1; then
        echo "PASS: $label"
        PASS=$((PASS + 1))
    else
        echo "FAIL: $label"
        FAIL=$((FAIL + 1))
    fi
}

echo "=== plcc-ng environment smoke test ($VARIANT) ==="

check "plcc-version runs"     "plcc-version"
check "plcc-make on PATH"     "command -v plcc-make"
check "plcc-scan on PATH"     "command -v plcc-scan"
check "plcc-parse on PATH"    "command -v plcc-parse"
check "plcc-rep on PATH"      "command -v plcc-rep"
check "Python 3.12+ present"  "python3 --version 2>&1 | grep -qE '3\.(1[2-9]|[2-9][0-9])'"
check "Java 21+ present"      "java --version 2>&1 | grep -qE '(openjdk|java) (2[1-9]|[3-9][0-9])'"
check "Node present"          "command -v node"
check "npm present"           "command -v npm"
check "git present"           "command -v git"

if [ "$VARIANT" = "plcc-ng-full" ]; then
    check "GHC present"   "command -v ghc || [ -e /usr/local/.ghcup/bin/ghc ] || [ -e \$HOME/.ghcup/bin/ghc ]"
    check "cabal present" "command -v cabal || [ -e /usr/local/.ghcup/bin/cabal ] || [ -e \$HOME/.ghcup/bin/cabal ]"
fi

echo ""
echo "Results: $PASS passed, $FAIL failed"
[ "$FAIL" -eq 0 ] || exit 1
```

- [ ] **Step 2: Write the sync and build scripts**

Create `scripts/sync-features.sh` (then `chmod +x`):

```bash
#!/bin/bash
# SPDX-FileCopyrightText: 2026 ourPLCC contributors
# SPDX-License-Identifier: GPL-3.0-or-later
# Copies the canonical feature source (src/plcc-ng) into each image
# workspace. The copies are gitignored; run this before any image build.
set -euo pipefail

cd "$(dirname "$0")/.."

for image in images/*/; do
    dest="${image}.devcontainer/features/plcc-ng"
    rm -rf "$dest"
    mkdir -p "$(dirname "$dest")"
    cp -R src/plcc-ng "$dest"
    echo "synced src/plcc-ng -> $dest"
done
```

Create `scripts/build-and-test.sh` (then `chmod +x`):

```bash
#!/bin/bash
# SPDX-FileCopyrightText: 2026 ourPLCC contributors
# SPDX-License-Identifier: GPL-3.0-or-later
# Builds one prebuilt-image variant and runs the smoke test inside it.
# Usage: build-and-test.sh <plcc-ng|plcc-ng-full> [image-tag]
set -euo pipefail

VARIANT="${1:?usage: build-and-test.sh <plcc-ng|plcc-ng-full> [image-tag]}"
TAG="${2:-ghcr.io/ourplcc/devcontainers/${VARIANT}:dev}"

cd "$(dirname "$0")/.."

bash scripts/sync-features.sh

devcontainer build \
    --workspace-folder "images/${VARIANT}" \
    --image-name "$TAG"

# Run the smoke test through the devcontainer CLI so feature-provided
# environment (containerEnv/remoteEnv, e.g. Java's PATH) is applied.
cp test/smoke-test.sh "images/${VARIANT}/smoke-test.sh"
devcontainer up --workspace-folder "images/${VARIANT}" --remove-existing-container
devcontainer exec --workspace-folder "images/${VARIANT}" bash smoke-test.sh "$VARIANT"
```

- [ ] **Step 3: Write the two image definitions**

Create `images/plcc-ng/.devcontainer/devcontainer.json` (note: no python feature — the plcc-ng feature's `dependsOn` provides Python 3.12):

```json
{
    "name": "PLCC-NG",
    "build": { "dockerfile": "Dockerfile" },
    "features": {
        "ghcr.io/devcontainers/features/java:1": { "version": "21" },
        "ghcr.io/devcontainers/features/node:1": { "version": "lts" },
        "./features/plcc-ng": { "version": "1.0.0" }
    }
}
```

Create `images/plcc-ng/.devcontainer/Dockerfile`:

```dockerfile
FROM mcr.microsoft.com/devcontainers/base:ubuntu
LABEL org.opencontainers.image.source="https://github.com/ourPLCC/plcc-ng-devcontainer"
LABEL org.opencontainers.image.description="Prebuilt devcontainer with plcc-ng, Python, Java, and JavaScript"
LABEL org.opencontainers.image.licenses="GPL-3.0-or-later"
```

Create `images/plcc-ng-full/.devcontainer/devcontainer.json`:

```json
{
    "name": "PLCC-NG (full)",
    "build": { "dockerfile": "Dockerfile" },
    "features": {
        "ghcr.io/devcontainers/features/java:1": { "version": "21" },
        "ghcr.io/devcontainers/features/node:1": { "version": "lts" },
        "ghcr.io/devcontainers-extra/features/haskell:2": {},
        "./features/plcc-ng": { "version": "1.0.0" }
    }
}
```

Create `images/plcc-ng-full/.devcontainer/Dockerfile`:

```dockerfile
FROM mcr.microsoft.com/devcontainers/base:ubuntu
LABEL org.opencontainers.image.source="https://github.com/ourPLCC/plcc-ng-devcontainer"
LABEL org.opencontainers.image.description="Prebuilt devcontainer with plcc-ng, Python, Java, JavaScript, and Haskell"
LABEL org.opencontainers.image.licenses="GPL-3.0-or-later"
```

- [ ] **Step 4: Gitignore the build-time copies**

Append to `.gitignore`:

```gitignore
# Synced/copied at build time by scripts — do not commit
images/*/.devcontainer/features/
images/*/smoke-test.sh
```

- [ ] **Step 5: Build and test the standard image**

Run: `bash scripts/build-and-test.sh plcc-ng`
Expected: build completes (~5–10 min cold), then smoke test output ends with `Results: 10 passed, 0 failed`. If a Java or Node check fails, run `devcontainer exec --workspace-folder images/plcc-ng bash -c 'echo $PATH'` to inspect the applied environment before changing anything.

- [ ] **Step 6: Build and test the full image**

Run: `bash scripts/build-and-test.sh plcc-ng-full`
Expected: 20–40 min; smoke test ends `Results: 12 passed, 0 failed`. If the GHC/cabal checks fail, find the install location with `devcontainer exec --workspace-folder images/plcc-ng-full bash -c 'ls /usr/local/.ghcup/bin ~/.ghcup/bin 2>&1'` and adjust only those two checks in `test/smoke-test.sh` to match reality.

- [ ] **Step 7: Record real image sizes**

Run: `docker image ls ghcr.io/ourplcc/devcontainers/plcc-ng ghcr.io/ourplcc/devcontainers/plcc-ng-full --format '{{.Repository}} {{.Size}}'`
Expected: two lines, roughly `~2GB` and `~7GB`. **Save these numbers** — Task 7's docs table must use them (replacing the `~2 GB` / `~7 GB` defaults written there).

- [ ] **Step 8: Update the root template**

Replace the entire content of the root `devcontainer.json`:

```json
// Template for student/course projects. Copy this to your project's
// .devcontainer/devcontainer.json and adjust as needed.
// This file is NOT used by this repository's own devcontainer.
// If your course uses Haskell, use image ghcr.io/ourplcc/devcontainers/plcc-ng-full:1
{
    "name": "My PLCC-NG Project",
    "image": "ghcr.io/ourplcc/devcontainers/plcc-ng:1"
}
```

- [ ] **Step 9: Commit**

```bash
git add images scripts test .gitignore devcontainer.json
git commit -m "feat: add plcc-ng and plcc-ng-full prebuilt image definitions"
```

---

### Task 4: CI workflow (PR builds)

**Files:**
- Modify: `.github/workflows/ci.yml` (full rewrite)

**Interfaces:**
- Consumes: `scripts/build-and-test.sh` (Task 3), `devcontainer features test` layout (Task 2).
- Produces: on every PR — feature tests, both images built and smoke-tested on amd64, pushed as `ghcr.io/ourplcc/devcontainers/<variant>:pr-{N}`, one PR comment listing both tags.

- [ ] **Step 1: Rewrite `.github/workflows/ci.yml`**

Replace the entire file with:

```yaml
# SPDX-FileCopyrightText: 2026 ourPLCC contributors
# SPDX-License-Identifier: GPL-3.0-or-later
name: CI

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  packages: write
  pull-requests: write

jobs:
  test-feature:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install devcontainer CLI
        run: npm install -g @devcontainers/cli

      - name: Test plcc-ng feature
        run: |
          devcontainer features test \
            --project-folder . \
            --features plcc-ng \
            --base-image mcr.microsoft.com/devcontainers/base:ubuntu

  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        variant: [plcc-ng, plcc-ng-full]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Free disk space for large image build
        if: matrix.variant == 'plcc-ng-full'
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/hostedtoolcache/CodeQL

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install devcontainer CLI
        run: npm install -g @devcontainers/cli

      - name: Build image and run smoke test
        run: |
          bash scripts/build-and-test.sh "${{ matrix.variant }}" \
            "ghcr.io/ourplcc/devcontainers/${{ matrix.variant }}:pr-${{ github.event.pull_request.number }}"

      - name: Push PR image
        run: docker push "ghcr.io/ourplcc/devcontainers/${{ matrix.variant }}:pr-${{ github.event.pull_request.number }}"

  comment:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Comment image tags on PR
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: [
                '🐳 **PR images built** (amd64 only):',
                '- `ghcr.io/ourplcc/devcontainers/plcc-ng:pr-${{ github.event.pull_request.number }}`',
                '- `ghcr.io/ourplcc/devcontainers/plcc-ng-full:pr-${{ github.event.pull_request.number }}`',
                '',
                'To test, point a devcontainer.json `image` at one of these tags.'
              ].join('\n')
            })
```

- [ ] **Step 2: Lint the workflow**

Run: `docker run --rm -v "$PWD":/repo --workdir /repo rhysd/actionlint:latest -color .github/workflows/ci.yml`
Expected: no output (exit 0). Fix anything it reports.

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: build and test feature and both images on PRs"
```

---

### Task 5: Release workflow (multi-arch publish)

**Files:**
- Modify: `.github/workflows/release.yml` (full rewrite)

**Interfaces:**
- Consumes: `scripts/build-and-test.sh`; semantic-release config `.releaserc.json` (unchanged); repo secret `RELEASE_TOKEN` (documented in Task 7).
- Produces: on push to `main` — per-arch candidate tags `ghcr.io/ourplcc/devcontainers/<variant>:<sha>-<amd64|arm64>`; after semantic-release publishes version `X.Y.Z`, manifest lists tagged `X.Y.Z`, `X.Y`, `X`, `latest` for both images; feature published to `ghcr.io/ourplcc/features/plcc-ng` whenever `src/plcc-ng/devcontainer-feature.json`'s version is new (the CLI skips already-published versions).

- [ ] **Step 1: Rewrite `.github/workflows/release.yml`**

Replace the entire file with:

```yaml
# SPDX-FileCopyrightText: 2026 ourPLCC contributors
# SPDX-License-Identifier: GPL-3.0-or-later
name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write
  packages: write
  pull-requests: write
  issues: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        variant: [plcc-ng, plcc-ng-full]
        runner: [ubuntu-latest, ubuntu-24.04-arm]
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Free disk space for large image build
        if: matrix.variant == 'plcc-ng-full'
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/hostedtoolcache/CodeQL

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install devcontainer CLI
        run: npm install -g @devcontainers/cli

      - name: Determine architecture
        id: arch
        run: echo "arch=$(dpkg --print-architecture)" >> "$GITHUB_OUTPUT"

      - name: Build candidate image and run smoke test
        run: |
          bash scripts/build-and-test.sh "${{ matrix.variant }}" \
            "ghcr.io/ourplcc/devcontainers/${{ matrix.variant }}:${{ github.sha }}-${{ steps.arch.outputs.arch }}"

      - name: Push candidate image
        run: docker push "ghcr.io/ourplcc/devcontainers/${{ matrix.variant }}:${{ github.sha }}-${{ steps.arch.outputs.arch }}"

  release:
    needs: build
    runs-on: ubuntu-latest
    outputs:
      published: ${{ steps.semantic.outputs.new_release_published }}
      version: ${{ steps.semantic.outputs.new_release_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_TOKEN }}

      - name: Run semantic-release
        id: semantic
        uses: cycjimmy/semantic-release-action@v4
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

  publish-images:
    needs: release
    if: needs.release.outputs.published == 'true'
    runs-on: ubuntu-latest
    strategy:
      matrix:
        variant: [plcc-ng, plcc-ng-full]
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Tag multi-arch manifests
        env:
          VERSION: ${{ needs.release.outputs.version }}
          IMAGE: ghcr.io/ourplcc/devcontainers/${{ matrix.variant }}
        run: |
          MAJOR=$(echo "$VERSION" | cut -d. -f1)
          MAJOR_MINOR=$(echo "$VERSION" | cut -d. -f1-2)
          docker buildx imagetools create \
            -t "$IMAGE:$VERSION" \
            -t "$IMAGE:$MAJOR" \
            -t "$IMAGE:$MAJOR_MINOR" \
            -t "$IMAGE:latest" \
            "$IMAGE:${{ github.sha }}-amd64" \
            "$IMAGE:${{ github.sha }}-arm64"

  publish-feature:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install devcontainer CLI
        run: npm install -g @devcontainers/cli

      - name: Publish plcc-ng feature
        # Skips versions that are already published.
        run: |
          devcontainer features publish \
            --registry ghcr.io \
            --namespace ourplcc/features \
            ./src
```

- [ ] **Step 2: Lint the workflow**

Run: `docker run --rm -v "$PWD":/repo --workdir /repo rhysd/actionlint:latest -color .github/workflows/release.yml`
Expected: no output (exit 0).

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/release.yml
git commit -m "ci: publish multi-arch images and the plcc-ng feature on release"
```

---

### Task 6: Weekly plcc-ng release check

**Files:**
- Delete: `.github/workflows/check-plcc-release.yml`
- Create: `.github/workflows/check-plcc-ng-release.yml`

**Interfaces:**
- Consumes: pinned version at jq path `.features["./features/plcc-ng"].version` in `images/plcc-ng/.devcontainer/devcontainer.json` and `images/plcc-ng-full/.devcontainer/devcontainer.json` (Task 3); secret `RELEASE_TOKEN`.
- Produces: a weekly `fix: update plcc-ng to X.Y.Z` PR touching both image configs.

- [ ] **Step 1: Replace the workflow**

```bash
git rm .github/workflows/check-plcc-release.yml
```

Create `.github/workflows/check-plcc-ng-release.yml`:

```yaml
# SPDX-FileCopyrightText: 2026 ourPLCC contributors
# SPDX-License-Identifier: GPL-3.0-or-later
name: Check plcc-ng Release

on:
  schedule:
    - cron: '0 9 * * 1'   # Every Monday at 09:00 UTC
  workflow_dispatch:        # Allow manual trigger for testing

permissions:
  contents: write
  pull-requests: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.RELEASE_TOKEN }}

      - name: Check for new plcc-ng release on PyPI
        env:
          GH_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        run: |
          LATEST=$(curl -fsSL https://pypi.org/pypi/plcc-ng/json | jq -r '.info.version')
          echo "Latest plcc-ng release: $LATEST"

          CURRENT=$(jq -r '.features["./features/plcc-ng"].version' \
            images/plcc-ng/.devcontainer/devcontainer.json)
          echo "Currently pinned: $CURRENT"

          if [ "$LATEST" = "$CURRENT" ]; then
            echo "Already up to date. No action needed."
            exit 0
          fi

          BRANCH="chore/update-plcc-ng-${LATEST}"

          # Idempotency guard: PR branch already exists
          if git ls-remote --exit-code --heads origin "$BRANCH" > /dev/null 2>&1; then
            echo "Branch '$BRANCH' already exists. PR already open."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "$BRANCH"

          for cfg in images/plcc-ng/.devcontainer/devcontainer.json \
                     images/plcc-ng-full/.devcontainer/devcontainer.json; do
            jq --arg v "$LATEST" \
              '.features["./features/plcc-ng"].version = $v' \
              "$cfg" > tmp.json
            mv tmp.json "$cfg"
          done

          git add images
          git commit -m "fix: update plcc-ng to ${LATEST}"

          # Race-condition guard: treat push failure as no-op
          git push origin "$BRANCH" || {
            echo "Push failed — branch may already exist. Exiting cleanly."
            exit 0
          }

          gh pr create \
            --title "fix: update plcc-ng to ${LATEST}" \
            --label "automated" \
            --body "Automated update: plcc-ng has a new release (${LATEST}).

          CI will build and test both images on this PR. Merge if green — merging triggers the release workflow, which publishes new versioned images automatically.

          PyPI release: https://pypi.org/project/plcc-ng/${LATEST}/" \
            --base main \
            --head "$BRANCH"
```

- [ ] **Step 2: Lint the workflow**

Run: `docker run --rm -v "$PWD":/repo --workdir /repo rhysd/actionlint:latest -color .github/workflows/check-plcc-ng-release.yml`
Expected: no output (exit 0).

- [ ] **Step 3: Commit**

```bash
git add .github/workflows
git commit -m "ci: check PyPI weekly for new plcc-ng releases"
```

---

### Task 7: Documentation (README, choosing guide, maintainer guide)

**Files:**
- Modify: `README.md` (full rewrite)
- Create: `docs/choosing-your-image.md`
- Create: `docs/maintainer-guide.md`

**Interfaces:**
- Consumes: measured image sizes from Task 3 Step 7 — **replace the `~2 GB` / `~7 GB` figures below with the measured values**.
- Produces: the two audience documents required by the spec, linked from the README.

- [ ] **Step 1: Rewrite `README.md`**

Replace the entire file with:

````markdown
# plcc-ng-devcontainer

Prebuilt [devcontainer](https://containers.dev/) images and a devcontainer
feature for [plcc-ng](https://github.com/ourPLCC/plcc-ng) — for use in courses
and assignments on GitHub Codespaces or VS Code.

## Quick Start

Add a `.devcontainer/devcontainer.json` to your project:

```json
{
  "name": "My PLCC-NG Project",
  "image": "ghcr.io/ourplcc/devcontainers/plcc-ng:1"
}
```

If your course implements language semantics in **Haskell**, use
`ghcr.io/ourplcc/devcontainers/plcc-ng-full:1` instead.

Open the project in [GitHub Codespaces](https://codespaces.github.com) or
[VS Code with the Dev Containers extension](https://marketplace.visualstudio.com/items?itemName=ms-vscode-remote.remote-containers).
plcc-ng, Python, Java, and JavaScript are ready to use (plus Haskell in `-full`).

**Not sure which image to pick?** See [Choosing your image](./docs/choosing-your-image.md).

## What's published here

| Artifact | Where |
|---|---|
| Standard image (plcc-ng, Python, Java, JavaScript) | `ghcr.io/ourplcc/devcontainers/plcc-ng` |
| Full image (standard + Haskell) | `ghcr.io/ourplcc/devcontainers/plcc-ng-full` |
| Devcontainer feature (installs only plcc-ng) | `ghcr.io/ourplcc/features/plcc-ng` |

## Contributing

1. Fork and clone this repository.
2. Open it in VS Code or Codespaces — the repo's devcontainer includes the
   devcontainer CLI and Docker.
3. Make your changes; test with `bash scripts/build-and-test.sh plcc-ng` and
   `devcontainer features test --project-folder . --features plcc-ng --base-image mcr.microsoft.com/devcontainers/base:ubuntu`.
4. Open a PR — CI tests the feature and builds both images as `pr-{N}` tags.

Use [Conventional Commits](https://www.conventionalcommits.org/):
`fix:` → patch, `feat:` → minor, `feat!:`/`BREAKING CHANGE:` → major.

See the [maintainer guide](./docs/maintainer-guide.md) for how releases work.
````

- [ ] **Step 2: Create `docs/choosing-your-image.md`**

````markdown
# Choosing Your Image

This guide is for students and faculty setting up a course or assignment
that uses [plcc-ng](https://github.com/ourPLCC/plcc-ng).

## Which image? One question

**Does your course implement language semantics in Haskell?**

- **No** → use the standard image (smaller, faster to start):

  ```json
  {
    "name": "My PLCC-NG Project",
    "image": "ghcr.io/ourplcc/devcontainers/plcc-ng:1"
  }
  ```

- **Yes** → use the full image (adds the Haskell toolchain):

  ```json
  {
    "name": "My PLCC-NG Project",
    "image": "ghcr.io/ourplcc/devcontainers/plcc-ng-full:1"
  }
  ```

Save the snippet as `.devcontainer/devcontainer.json` in your project.

## What's inside

| | `plcc-ng` (~2 GB) | `plcc-ng-full` (~7 GB) |
|---|---|---|
| plcc-ng (all `plcc-*` commands) | ✅ | ✅ |
| Python 3.12 | ✅ | ✅ |
| Java 21 | ✅ | ✅ |
| JavaScript (Node LTS) | ✅ | ✅ |
| Haskell (GHC, cabal) | ❌ | ✅ |

Both images are published for amd64 (Codespaces) and arm64 (Apple Silicon).

## Picking a version tag

| Tag | Meaning |
|---|---|
| `latest` | Most recent release |
| `1` | Latest `1.x.x` release |
| `1.2` | Latest `1.2.x` release |
| `1.2.3` | Exact release (immutable) |

**Recommendation for courses:** pin the major tag (e.g. `:1`) for the
semester. You get plcc-ng updates and security patches automatically but are
protected from breaking changes mid-course.

## Custom setups: the feature

If the prebuilt images don't fit (different Java version, extra tools, other
base image), compose your own devcontainer with the plcc-ng feature. It
installs plcc-ng and Python; add language features as needed:

```json
{
  "name": "My Custom PLCC-NG Project",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/ourplcc/features/plcc-ng:1": {},
    "ghcr.io/devcontainers/features/java:1": { "version": "21" }
  }
}
```

The feature's `version` option pins a plcc-ng release from
[PyPI](https://pypi.org/project/plcc-ng/), e.g.
`{ "version": "1.0.0" }`; the default is `latest`.

Note: the first Codespaces start of a feature-composed devcontainer builds
the container from scratch (several minutes). The prebuilt images exist to
avoid exactly that — prefer them when they fit.
````

- [ ] **Step 3: Create `docs/maintainer-guide.md`**

````markdown
# Maintainer Guide

How the artifacts in this repository are produced, versioned, and released.

## Source layout

| Path | What it is |
|---|---|
| `src/plcc-ng/` | The devcontainer feature (canonical source) |
| `test/plcc-ng/` | Feature tests (`devcontainer features test` layout) |
| `images/plcc-ng/`, `images/plcc-ng-full/` | Prebuilt image definitions (devcontainer.json + label Dockerfile) |
| `test/smoke-test.sh` | In-container smoke test shared by both images |
| `scripts/sync-features.sh` | Copies `src/plcc-ng` into each image workspace (copies are gitignored) |
| `scripts/build-and-test.sh` | Builds one image variant and smoke-tests it — same entry point locally and in CI |

The images consume the **local** feature copy (synced by
`scripts/sync-features.sh`), never the published one — so a PR that changes
the feature is tested end-to-end before anything is published.

## How a release happens

1. A PR merges to `main` with a Conventional Commit message
   (`fix:` → patch, `feat:` → minor, `feat!:` → major).
2. The **Release** workflow's `build` job builds both image variants on
   native amd64 and arm64 runners, smoke-tests each, and pushes per-arch
   candidate tags `{sha}-amd64` / `{sha}-arm64`.
3. The `release` job runs semantic-release: computes the next version, tags
   the repo, updates `CHANGELOG.md`, creates the GitHub release.
4. The `publish-images` job stitches the candidate tags into multi-arch
   manifests tagged `X.Y.Z`, `X.Y`, `X`, and `latest` for both images.
5. The `publish-feature` job runs `devcontainer features publish` for
   `src/`; it publishes only if `devcontainer-feature.json`'s `version` has
   not been published before, so it is a no-op on most releases.

Image versions come from semantic-release; the **feature is versioned
independently** — bump the `version` field in
`src/plcc-ng/devcontainer-feature.json` in the same PR as any feature change.

## Weekly plcc-ng bump

`check-plcc-ng-release.yml` runs Mondays 09:00 UTC: it compares the latest
PyPI release of plcc-ng against the version pinned in both image configs
(`.features["./features/plcc-ng"].version`) and opens a
`fix: update plcc-ng to X.Y.Z` PR when they differ. Review CI and merge if
green. The feature itself is not touched (its default stays `latest`).

## Testing a PR build

CI pushes each PR's images as
`ghcr.io/ourplcc/devcontainers/{plcc-ng,plcc-ng-full}:pr-{N}` (amd64 only)
and comments the tags on the PR. Point any devcontainer.json `image` at one
of those tags to try it.

## Local development

```bash
# Feature tests (fast-ish; builds small test images)
devcontainer features test --project-folder . --features plcc-ng \
  --base-image mcr.microsoft.com/devcontainers/base:ubuntu

# Build + smoke test an image variant (plcc-ng-full takes 20–40 min)
bash scripts/build-and-test.sh plcc-ng
bash scripts/build-and-test.sh plcc-ng-full

# REUSE/license lint
bash scripts/reuse.sh lint
```

The repo's own devcontainer (`.devcontainer/devcontainer.json`) includes
Node, Docker-in-Docker, and the devcontainer CLI, so all of the above works
inside a Codespace.

## Initial repository setup (bootstrap)

One-time steps when this repository is first published to GitHub:

1. Create `ourPLCC/plcc-ng-devcontainer` and push `main` (no tags — the
   first release must compute as 1.0.0).
2. Add the `RELEASE_TOKEN` repository secret: a GitHub App token (preferred)
   or classic PAT with `repo` + `write:packages`. Required because
   `GITHUB_TOKEN` cannot push semantic-release's changelog commit to the
   protected `main` branch.
3. Protect `main` (require PRs and green CI).
4. Merge any PR with a `feat:` commit → first release publishes everything.
5. In the ourPLCC org package settings, make these GHCR packages **public**:
   `devcontainers/plcc-ng`, `devcontainers/plcc-ng-full`,
   `features/plcc-ng`. (New GHCR packages default to private.)

## Housekeeping

- Per-arch candidate tags (`{sha}-amd64`/`{sha}-arm64`) accumulate in GHCR.
  They are harmless; delete old untagged/candidate versions occasionally via
  the package settings UI if clutter bothers you.
- Renovate/Dependabot are not configured; the base image and language
  feature versions float by design (`base:ubuntu`, Node `lts`), pinned only
  where reproducibility matters (plcc-ng, Java major, Python minor).
````

- [ ] **Step 4: Verify internal links and remembered sizes**

Run: `grep -n "choosing-your-image\|maintainer-guide" README.md`
Expected: both links present. Confirm the sizes in `docs/choosing-your-image.md` match Task 3 Step 7's measurements.

- [ ] **Step 5: Commit**

```bash
git add README.md docs/choosing-your-image.md docs/maintainer-guide.md
git commit -m "docs: add image-choosing guide and maintainer guide for plcc-ng"
```

---

### Task 8: Final sweep and verification

**Files:**
- Possibly modify: any file still referencing the old identity.

**Interfaces:**
- Consumes: everything above.
- Produces: a repo with no stale plcc-devcontainer references and a clean REUSE lint.

- [ ] **Step 1: Sweep for stale references**

Run: `grep -rn "plcc-devcontainer" --exclude-dir=.git --exclude-dir=superpowers . ; grep -rniE "ourplcc/plcc([^-]|$)" --exclude-dir=.git --exclude-dir=superpowers .`
Expected: no output from either. (`docs/superpowers/` is excluded — historical specs/plans legitimately mention the old names.) Fix any hit and amend into an appropriate `chore:` commit.

- [ ] **Step 2: REUSE lint**

Run: `bash scripts/reuse.sh lint`
Expected: `Congratulations! Your project is compliant with version 3.3 of the REUSE Specification :-)` (version number may differ). Fix any non-compliant file it lists.

- [ ] **Step 3: Re-run the fast test suites**

Run: `devcontainer features test --project-folder . --features plcc-ng --base-image mcr.microsoft.com/devcontainers/base:ubuntu`
Expected: PASS.
Run: `bash scripts/build-and-test.sh plcc-ng`
Expected: `Results: 10 passed, 0 failed` (mostly cached, fast).

- [ ] **Step 4: Verify working tree is clean and history is coherent**

Run: `git status --short` — expected: empty.
Run: `git log --oneline -12` — expected: the commits from Tasks 1–7 in order, all Conventional Commits.
Run: `git tag -l` — expected: empty.

- [ ] **Step 5: Commit any sweep fixes**

Only if Step 1 or 2 required changes:

```bash
git add -A
git commit -m "chore: remove remaining plcc-devcontainer references"
```
